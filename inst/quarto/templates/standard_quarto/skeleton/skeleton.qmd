---
title: "Preliminary Report: The real title"
author: "First and Last Name"
date: "`r Sys.Date()`"
date-format: long
format:
  html:
    theme: united
    title-block-banner: false
    grid:
      body-width: 900px
    toc: true
    toc-depth: 3
    toc-location: left
    toc-expand: true
    smooth-scroll: true
    code-fold: false
    fig-align: center
    fig-width: 6.5
    css: style.css
    tbl-colwidths: false
crossref:
  fig-title: "Figure"
  tbl-title: "Table"
bibliography: references.bib # see https://quarto.org/docs/authoring/citations.html
csl: https://www.zotero.org/styles/apa
link-citations: true
editor: visual
---

```{r setup, echo=FALSE, include=FALSE}
library(knitr)
library(kableExtra)
library(scales)
library(ggplot2)
library(dplyr)
library(tidyr)
library(trcpetc)


knitr::opts_chunk$set(
  fig.align = "center",
  dev = "png",
  dev.args = list(pointsize = 11),
  fig.show = "hold",
  out.extra = 'style="border:20px solid white;"'
)

knit_hooks$set(
  inline = function(x) if (is.numeric(x)) round(x, 3) else x
)

options(knitr.kable.NA = "")
options(scipen = 999)

```

```{r, echo=F}
set.seed(123)

#load('2 - descriptive.RData')
cars$study_id = 1:length(cars$speed)
cars$colour  = factor(ifelse(cars$speed >10,'red','blue'))

tab_overall = trcpetc::table_one(cars)
tab_group   = trcpetc::table_one(cars, group = colour)

```

# Introduction

Quarto documents are very similar to Rmd documents. The advantage is that you get two editors that you can switch between in the toggle top left (Source vs. Visual). It can be convenient to use the Visual one, for example when 'hand-writing' a table. You can also access other formatting help in the task bar above.

Referencing literature works the same as in Rmd (brackets and at), for example [@Gao2026] .

| Abbreviation | Description         |
|--------------|---------------------|
| Rmd          | R-markdown document |
| Qmd          | Quarto document     |

: Table that has quickly been added using the "Table" toggle in the task bar

In this document, we also use the new trcpetc workflow for tables, i.e. table_one() to get the raw descriptives, then kable_table_one to automatically make the header nice etc.

# Methods

Clinical characteristics were summarized using descriptive statistics. Continuous variables were characterized using median and interquartile range; dichotomous or polytomous variables were characterized using frequencies.

Between-group comparisons were evaluated using Wilcoxon rank-sum tests for continuous variables and Fisher's exact tests for dichotomous and polytomous variables.


Cumulative incidence curves were used ....

Analyses were performed using `r R.version.string` (R Foundation for Statistical Computing, Vienna, Austria).


# Results

## Descriptive analysis

You can also enter R-code with the typical "`r` " if you use the "Source" Editor.

## Including Tables

Table chunk options need to include a reference name for the table, only then will it automatically give it a Table number.

```{r tbl-cars-overall, echo = FALSE}
#| tbl-cap: "Summary of cars"

kable_table_one(tab_overall)

```


```{r tbl-cars-group, echo = FALSE}
#| tbl-cap: "Summary of cars by colour"

kable_table_one(tab_group)

```


## Including Figures

See @fig-cars for the relationship between speed and distance.

```{r fig-cars, echo = F }
#| fig-cap: "Similar to tables, the caption needs to be added below the chunk option like this. If you add the figure reference (fig-cars) in the chunk option, it will automatically number them correctly."

plot(cars$speed, cars$dist)
```
